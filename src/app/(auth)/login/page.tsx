/**
 * Placeholder login route. Auth UI is wired up in a later pass — this file
 * exists to establish the `(auth)` route group.
 */
export default function LoginPage() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-sm flex-col justify-center gap-2 p-8">
      <h1 className="text-xl font-semibold">Sign in</h1>
      <p className="text-sm text-zinc-500">
        Authentication flow not implemented yet.
      </p>
    </main>
  );
}
